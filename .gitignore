# Environment variables - NEVER upload to GitHub
.env

# Node dependencies - too large, installed via npm install
node_modules/

# Node package files - not needed on GitHub since Vercel serves static HTML only
package.json
package-lock.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
